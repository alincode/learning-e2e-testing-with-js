var assert = require('assert');

describe('第一個單元測試', function() {

  it('加', function() {
    assert.equal(1 + 2, 3);
  });

  it('減', function() {
      assert.equal(2 - 1, 2);
    //   throw new Error();
  });

  it('乘', function() {
  });

  it('除', function() {
  });

});